import java.util.*;

public class Main5 {
    public static void main(String[] args) {

        Scanner input = new Scanner(System.in);
        int n = input.nextInt();

        if (n >= 3 && n <= 15) {
            System.out.print("Invalid n value.");
        }

        for (int i = 0; i < n; i++) {
            double inDig = input.nextInt();
            double[] knownDig = {inDig};
        }

        for (int j = 0; j < n; j++) {
            // double temp = knownDig[n.lenth-1];

        }

        input.close();

        }
    }